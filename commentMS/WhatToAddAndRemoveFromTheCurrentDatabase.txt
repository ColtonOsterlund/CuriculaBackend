Remove:
comment table

parentComment table: foreign key cascade on delete
childComment table: parentID, childID cascade on delete, parentID cascade on delete

Add:

parentComment table: commentContent, userID, timeStamp, childCount, voteCount
childComment table: (everything that's added in parentComment table but childCount)